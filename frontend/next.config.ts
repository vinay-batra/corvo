import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Vercel handles bundling; standalone is only for self-hosted Docker deploys
  // output: "standalone",
};

export default nextConfig;
