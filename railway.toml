[deploy]
startCommand = "PYTHONUNBUFFERED=1 bash -c 'cd $([ -f main.py ] && echo . || ([ -d backend ] && echo backend || echo .)) && uvicorn main:app --host 0.0.0.0 --port $PORT'"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
